# Production Environment Variables
# Copy this file to .env.local and fill in the values

# ============================================
# AUTHENTICATION (REQUIRED)
# ============================================

# NextAuth Configuration
# For production, set NEXTAUTH_URL to your deployment URL
# For local development, use http://localhost:3000
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-a-secure-random-string-here

# GitHub OAuth (REQUIRED - Only authentication method)
# Get these from https://github.com/settings/developers
# Create a new OAuth App with:
# - Homepage URL: http://localhost:3000 (or your deployment URL)
# - Authorization callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ============================================
# DATABASE (OPTIONAL)
# ============================================

# Supabase (OPTIONAL - for data persistence)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Google Gemini API (REQUIRED for AI features)
GOOGLE_GEMINI_API_KEY=your-gemini-api-key

# ElevenLabs API (OPTIONAL - for voice features)
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# Leonardo AI API (OPTIONAL - for image generation)
LEONARDO_API_KEY=your-leonardo-api-key

# ============================================
# DEPLOYMENT NOTES
# ============================================
# 
# For Vercel/Production Deployment:
# 1. Add all environment variables in your hosting dashboard
# 2. NEXTAUTH_URL must match your deployment URL exactly (e.g., https://your-app.vercel.app)
# 3. Generate NEXTAUTH_SECRET using: openssl rand -base64 32
# 4. Update GitHub OAuth App callback URL to: https://your-app.vercel.app/api/auth/callback/github
# 5. GitHub OAuth is the ONLY authentication method - no email/password login

# Required: Google Gemini API (for all AI features)
GOOGLE_GENERATIVE_AI_API_KEY=

# Optional: Text-to-Speech (OpenAI TTS is used if OPENAI_API_KEY is provided)
TTS_API_KEY=

# Required: Vapi AI Voice Agent (for voice interviews)
NEXT_PUBLIC_VAPI_WEB_TOKEN=
NEXT_PUBLIC_VAPI_WORKFLOW_ID=

# Note: This platform uses Google Gemini exclusively for all AI features
# including question generation, feedback, and AI responses

# Note: Image generation has been removed from this version

# Required: Application Settings
NEXT_PUBLIC_APP_URL=
JWT_SECRET=

# Optional: Email Configuration
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Optional: Analytics
GOOGLE_ANALYTICS_ID=
MIXPANEL_TOKEN=